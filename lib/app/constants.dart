import 'package:flutter/material.dart';

class AppConstants {
  // Colors
  static const Color primaryGreen = Color(0xFF006B3F);
  static const Color black = Color(0xFF1A1A1A);
  static const Color white = Color(0xFFFFFFFF);
  static const Color gold = Color(0xFFC9A227);

  // Strings
  static const String appTitle = 'BudgetWatch Kenya';
  static const String tagline = 'Follow Every Shilling';
  static const String motto = 'Sovereignty Belongs to the People';

  // Dimensions
  static const double defaultPadding = 16.0;
  static const double defaultBorderRadius = 8.0;

  // Duration
  static const Duration animationDuration = Duration(milliseconds: 300);
}
